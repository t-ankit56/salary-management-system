"""App config for ``imports`` (bulk roster upload)."""

from django.apps import AppConfig


class ImportsConfig(AppConfig):
    """Django app configuration for ``imports``."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "imports"
